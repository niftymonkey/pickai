settings.local.json
.env

node_modules/
dist/

*.tsbuildinfo
.turbo/

examples/_api-cache.json

# Bespoke, per-project model-selection scripts generated by /pick-model.
# These are personal one-offs, not curated docs examples. Copy them into
# the target project; they don't belong in pickai's history.
picks/

# Throwaway scripts /pick-model writes to answer one-off questions. They live
# in the repo so "pickai" resolves, but they are disposable, not deliverables.
scratch/

