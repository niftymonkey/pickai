// Page header: the wordmark. The counts live in the census receipt above the table,
// where a fact about the data belongs; printing them here too printed one number twice.

const CatalogHeader = () => (
  <header className="mb-5">
    <h1 className="text-2xl font-semibold tracking-tight">pickai</h1>
  </header>
);

export { CatalogHeader };
