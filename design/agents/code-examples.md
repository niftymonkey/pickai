# Coding rule examples

The pinned evidence for the rules in `.claude/rules/code-core.md` and `.claude/rules/code-typescript.md`. Each entry carries one rule's specimen, its chosen form as real code, and its rejected forms. Read a rule's entry when the rule alone leaves the path forward unclear.

An entry enters only through a verified round: the same real code from this repo written multiple ways side by side, with Mark verifying which way is the standard. See the Coding standards section of `feature-playbook.md`.

The rules arrived from `the-cabinet` on 2026-08-31. That repository's specimens are PixiJS game code and were deliberately left behind.

No entries yet.
